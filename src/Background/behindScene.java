package Background;
import javax.swing.*;
import java.awt.*;
import java.io.*;
import java.net.*;

public class behindScene
{

   /* public void test()
    {
        try {
            //reading the data sent from a user
            Socket chatAdress = new Socket("127.0.0.1", 5000);
            InputStreamReader stream = new InputStreamReader(chatAdress.getInputStream());
            BufferedReader reader = new BufferedReader(stream);
            String message = reader.readLine();

            //sending the data from a user
            PrintWriter writer = new PrintWriter(chatAdress.getOutputStream());
            writer.println("message to send");
            writer.print("another message");
            reader.close();
        }
        catch (IOException ex)
        {
            ex.printStackTrace();
        }
    }*/

   //creating chat client


}
